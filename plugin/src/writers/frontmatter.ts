// Stage 4 — YAML frontmatter writer
// TODO: inject status, difficulty, lessonId, moduleId, generationMode, sourceRefs, timestamps
export function buildFrontmatter(
  _fields: Record<string, unknown>,
): string {
  throw new Error('Frontmatter writer not yet implemented');
}
