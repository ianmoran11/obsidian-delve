// Stage 4 — Navigation link writer
// TODO: inject breadcrumbs, previous/next lesson links, and course index back-links
export function buildNavLinks(
  _prev: string | null,
  _next: string | null,
  _indexPath: string,
  _modulePath: string,
): string {
  throw new Error('Navigation writer not yet implemented');
}
