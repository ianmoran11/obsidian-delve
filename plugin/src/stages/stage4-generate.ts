// Stage 4 — Iterative Content Generation
// Input:  Stage3Cache (curriculum)
// Output: Lesson .md files, module MOCs, Course Index, Canvas, frontmatter, nav links
// TODO: implement
import type { CourseId } from '../interfaces';

export async function runStage4(_courseId: CourseId): Promise<void> {
  throw new Error('Stage 4 not yet implemented');
}
