import { describe, it } from 'vitest';

// Placeholder test file — stages 1–4 tests will be added here as each stage is implemented

describe('stages 1–4 (stub)', () => {
  it.todo('Stage 1: concept extraction produces correct schema');
  it.todo('Stage 2: proficiency map covers all concepts');
  it.todo('Stage 3: curriculum respects proficiency thresholds');
  it.todo('Stage 4: lesson files are written to the correct vault paths');
});
